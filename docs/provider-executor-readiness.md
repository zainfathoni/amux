# Provider executor readiness

This matrix is the versioned source of truth for when `/amux-tycho`, `/amux-claude`, and `/amux-pi` may be used. It records contract readiness, not a promise that a provider account has current entitlement, quota, or availability. Every route remains explicit-owner-only and must still pass its live preflight.

Statuses:

- **Proven experimental** — at least one bounded real run established the named route; each later run still requires preflight and exact authorization.
- **Conditional** — the contract is implemented for the stated narrow task shape, but host, model, capacity, or runtime evidence remains run-specific.
- **Runtime-unverified** — static recipe/tests exist, but the required complete real runtime evidence has not been accepted.
- **Blocked** — the route must not launch; a named contract or authority boundary is missing.
- **Unsupported** — no route grants this authority.

| Provider route | Status | Execution location | Allowed work | Exact runtime/model | Required authority and principal limits | Evidence / next gate |
|---|---|---|---|---|---|---|
| `/amux-tycho` semantic-report receipt/inbox | Conditional | Owner-private machine-local compatibility transport; execution uses an explicitly owner-selected existing route or one owner-authorized prepared route created without provider execution | For normal explicit owner-authorized use with an existing real native Amp coordinator: bind that coordinator, the exact producer route, and the task/artifact digest; create the receipt before the first run; submit one typed `complete` or `blocked` report; explicitly consume it; then separately acknowledge it. Route preparation creates at most one dormant project/agent, stops on ambiguity, and permits no fallback. Review validation selects either dual-local same-head proof or an immutable remote artifact pinned by repository, PR, full head SHA, and full tree SHA. Existing receipts use exactly `created → valid_report → delivered → acknowledged|abandoned`; notification uncertainty and terminal cleanup status are separate. [#327](https://github.com/zainfathoni/amux/issues/327)'s historical local-worker workflow was completed by merged PR #361 and is not a current native Amp coordinator gate for [#328](https://github.com/zainfathoni/amux/issues/328) | Tycho may route Claude or Pi, but the bridge makes no provider/model identity claim and owns no process lifecycle | Tycho has `report_only` authority; the real Amp thread retains coordinator/consume/ack authority; task-specific artifact checks do not attest the route, and route-specific host/model checks do not enlarge the receipt contract; Tycho never mutates GitHub reviews | The accepted [Karsa/nix-home Opus 5 lifecycle](https://github.com/zainfathoni/nix-home/issues/13#issuecomment-5248690973) completed create → separate-process recover → typed report → consume/`delivered` → separate acknowledge/`acknowledged` → terminal cleanup and closed [#323](https://github.com/zainfathoni/amux/issues/323). Retain this bridge until one ordinary field run returns exactly one schema-valid bounded `complete|blocked` result, authenticated and correlated to the invoking Amp request/thread, directly to that caller through the exact selected route without transcript/log mining or an Amux receipt. That direct return establishes delivery and has no separate Amux consume/acknowledge step; Amp still independently verifies findings and owns shared mutation. Multi-cycle and other formal-readiness work remains optional promotion policy for this compatibility route. |
| Claude local Darwin read-only thinker | Blocked | Historical Darwin physical-host route only | No new work | Historical model bindings remain evidence only | Receipt ownership requires removed core Amux worker lifecycle; reject before receipt creation, launch, parking, retirement, or teardown | Prior [#149](https://github.com/zainfathoni/amux/issues/149) and [#305](https://github.com/zainfathoni/amux/issues/305#issuecomment-5115358465) runs remain historical evidence. Preserve existing receipts/processes/artifacts until a separately authenticated replacement is proven and accepted. |
| Claude local Linux read-only thinker | Blocked | Historical Linux physical-host route only | No new work | Historical model bindings remain evidence only | Same removed worker-lifecycle blocker as Darwin; synthetic process-identity coverage does not reopen admission | Preserve existing evidence; no launch, parking, retirement, or teardown route is currently authorized. |
| Claude local Darwin mutating Stage A | Blocked | Historical Darwin physical-host route only | No new work | Historical exact-model binding remains evidence only | Removed core worker report/finish/teardown lifecycle leaves no valid completion route; reject before worktree preparation or provider mutation | [#253](https://github.com/zainfathoni/amux/issues/253) remains historical evidence only. |
| Claude local Linux mutation | Blocked | Linux physical host | None | No accepted mutating model route | Linux mutation remains fail-closed; removed worker lifecycle is an additional blocker | [#255](https://github.com/zainfathoni/amux/issues/255). |
| Claude fresh-Orb read-only Opus | Proven experimental | Fresh Amp Orb in a configured Workspace Project | Bounded headless no-tool or read-only task, no repository mutation | Exact `claude-opus-4-8` through project-secret OAuth | Fresh Orb; API-key/custom-provider conflicts block; single-key model usage validation; result remains untrusted | [#205](https://github.com/zainfathoni/amux/issues/205). Opus 5 is not approved by this distinct recipe. |
| Claude fresh-Orb mutation | Blocked | Fresh Amp Orb | None | None | Missing authenticated native launch, mutation-authority, artifact-import, acknowledgement, archive, and cleanup receipts | [#254](https://github.com/zainfathoni/amux/issues/254). Never substitute the read-only Orb or local tmux route. |
| Claude operator-assisted local tmux adoption | Proven experimental | Explicitly selected physical host | Coordinate one exact owner-created semantic tmux window under the adoption contract | Exact owner-confirmed `claude-opus-5` only; omission, aliases, defaults, fallback, and alternatives reject | Exact host/session/window and process identity; no implicit process creation, Orb fallback, or generalized managed delegation | [#291](https://github.com/zainfathoni/amux/issues/291) records bounded read-only, mutation, and decommission evidence; every adoption still needs exact owner authorization. |
| Pi physical-host bounded replacement | Proven experimental | Trusted physical host, dedicated clean worktree | One strictly bound replacement of one tracked file, followed by independent Amp review/tests | `@earendil-works/pi-coding-agent` 0.82.1, Node `>=22.19.0`, exact `openai-codex/gpt-5.3-codex-spark` through owner OAuth | One inference attempt; no tools/session/extras/retry/fallback; no commit/push authority; ordinary managed provider catalog cache only under the documented metadata bounds | [#293](https://github.com/zainfathoni/amux/issues/293) and [#295](https://github.com/zainfathoni/amux/pull/295) record one bounded physical OAuth run; useful replacement acceptance remains coordinator-owned. |
| Pi fresh-Orb Spark | Runtime-unverified | Fresh Linux x86_64 Amp Orb | Bounded no-tool marker and useful read-only task only | Exact Spark selector through owner-operated ChatGPT Codex OAuth | No API keys or credential transfer; trusted before/after quota evidence; logout and experiment-owned cleanup required | [#206](https://github.com/zainfathoni/amux/issues/206) owns the remaining real runtime validation. |
| Pi general repository mutation | Unsupported | Any | No multi-file, arbitrary edit, worktree, commit, push, PR, or merge authority | None | The one-file physical helper and read-only Orb recipe cannot be widened by analogy | Requires a separately designed and accepted authority contract. |

## Maintenance rules

Update this matrix in the same pull request that changes a route's model allowlist, authority, platform, runtime pin, or promotion status. Link the governing GitHub issue; do not close a runtime-evidence issue from static tests alone. Historical proposals remain evidence, while this table states current usability.

A newer CLI exposing a flag or model name is not enough to promote a row. Model support requires an exact approved identifier and complete binding through the route's policy and receipt chain. Runtime acceptance additionally requires the row's real authentication, entitlement, model-identity, output, lifecycle, and cleanup evidence.
