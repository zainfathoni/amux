# Machine-local Claude mutating delegation

This historical helper route is closed to new work. It depended on an Amux worker as lifecycle owner and on core report/finish/teardown transitions that no longer exist.

Do not prepare a provider worktree, create a receipt, launch Claude, grant write authority, accept a handoff, park or retire a process, or invoke an older helper path. Native Amp owns new task coordination and repository mutation. The fresh-Orb mutating route remains separately blocked unless its documented authenticated authority adapters are implemented and accepted; do not treat that route as a fallback.

Preserve any existing private receipt, commit, worktree, report, runtime state, process, artifacts, and origin fence. Use only the read-only diagnosis in [`claude-delegation-recovery.md`](claude-delegation-recovery.md).
